#pragma once

#include <string>
#include <vector>
#include "NFA.h"

class DFA
{
	public:
		DFA() = default;
		DFA(const DFA &other) = default;
		DFA &operator=(const DFA &other) = default;
		~DFA() = default;

	private:
};
