#pragma once

#include <string>
#include <vector>
#include "Tree.h"

class NFA
{
	public:
		NFA() = default;
		NFA(const NFA &other) = default;
		NFA &operator=(const NFA &other) = default;
		~NFA() = default;

	private:
};
